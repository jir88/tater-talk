# tater-talk
Simple LLM chat interface optimized for small models on poor hardware. Uses jir88/root-cellar to provide chat memory. User interface is written with the NiceGUI library. Uses openai library to connect to compatible clients. Works best with llama-swap or llama.cpp.
